# @acme/mobile

React Native app placeholder. Initialize with Expo or React Native CLI when ready:

```bash
# Using Expo
npx -y create-expo-app@latest ./

# Using React Native CLI
npx -y @react-native-community/cli init AcmeMobile --directory ./
```
