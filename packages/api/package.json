{
  "name": "@acme/api",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@acme/types": "workspace:*",
    "zod": "^3"
  },
  "devDependencies": {
    "@acme/config": "workspace:*",
    "typescript": "^5"
  }
}
