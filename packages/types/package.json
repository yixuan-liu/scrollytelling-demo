{
  "name": "@acme/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3"
  },
  "devDependencies": {
    "@acme/config": "workspace:*",
    "typescript": "^5"
  }
}
